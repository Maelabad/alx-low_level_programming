#include "main.h"

/**
* _isupper - Check if a Character is upper
* @c: An integer representing a character
* Return: 0
*/

int _isupper(int c)
{
	if (c >= 65 && c <= 91)
	{
		return (1);
	}
	return (0);
}

