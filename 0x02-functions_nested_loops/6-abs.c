
#include"main.h"
/**
* _abs - Return the absolute  value of the number
* @n: variable representing the number
* Return: n or -n
*/

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
